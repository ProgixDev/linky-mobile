import { HelpScreen } from '@/features/support';

/**
 * Routes stay THIN. `/help` hosts the support contacts + FAQ. Linked from Profil.
 */
export default function HelpRoute() {
  return <HelpScreen />;
}
