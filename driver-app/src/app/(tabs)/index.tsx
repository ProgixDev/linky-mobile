import { DeliveriesScreen } from '@/features/deliveries';

/**
 * Accueil (left tab) = the courier's deliveries worklist. Routes stay THIN.
 */
export default function AccueilRoute() {
  return <DeliveriesScreen />;
}
